# CineHarbor Next Actions

0. ~~提交并推送本次 Agnir 初始化~~ ✅ 已完成（2026-08-31，7 仓 commit + push 到 `github.com-matt`）。
1. ~~六仓尚未 push~~ ✅ 已完成（2026-08-31 全部推送；README 中 GitHub 链接已生效）。
2. P0–P6 均已落地；当前续作 douban-imdb-rt（见 3）。
3. ~~跟进 `docs/PLAN.md` 开放问题 #1（许可证）~~ ✅ 已解决（2026-08-28）。
4. 各子仓后续动作由其各自 Agnir `.agnir/next-actions.md` 维护。
5. douban-imdb-rt：P0 统一评分聚合层 + P1 IMDb 官方 datasets 零 key 已在 `cineharbor-web` 落地；剩余 P2（RT 真实数据源 + 播放详情页三源评分区）、P3 由 `cineharbor-web/.agnir/next-actions.md` 跟踪。
6. **待推送**：本仓 `main` 领先 `origin/main` 2 个提交（`a3cf58b` checkpoint 记录 douban-imdb-rt P0 与待推送提交 / `64b0b85` docs: douban-imdb-rt P0 落地…）；`cineharbor-web` 领先若干提交（ratings P0 / 品牌构建修复 / 主题色单一来源 / checkpoint / IMDb P1）。推送走 `github.com-matt`（需用户明确授权后再 push）。
7. ~~Web 数据面收敛（ADR-0005 双表面/native RPC）~~ → 已被 ADR-0006 取代：终态改为 Stremio 式（core WASM 化 + 抓取/代理外置 addon）。
8. **终态对齐 Stremio 执行（ADR-0006）**：阶段 0 ✅；阶段 1 进展（core 纯化 / `model`+`sync` 纯模型入 core ✅ / `core::transport::HttpClient` trait + `native-http` ✅ / `core::addons::RemoteAddon` 派发 + merge 聚合 ✅ / `core::storage` Storage trait + `native-storage`(sqlite) ✅ → 阶段 1 完成）；阶段 3 预研 ✅；**P3 传输 ✅**（core-web `FetchHttpClient` = fetch 版 HttpClient + `addon_{manifest,catalog,meta,streams}_json` 四桥；`node_addon_flow.js` mock addon 端到端走通 catalog→meta→streams）。聚合 fan-out 已收口（addon-host catalog/streams 走 core merge）。阶段 2 进展：douban ✅ / live ✅ / vod ✅（三个 standalone 抓取 addon，均带媒体转链）；媒体代理 ✅（`cineharbor-media` serve 转链 router，mock 上游端到端测试）。剩 local-service 移除内置版（P4）。阶段 3 进展：core-web fetch HttpClient + 四桥 + node mock addon 端到端 ✅；web 侧 WASM 构建/桥骨架 ✅（`cineharbor-web/scripts/build-core-wasm.mjs` + `src/lib/core/bridge.ts`，jest/tsc 验证）。续：web 侧 Worker 挂 core（`public/core-worker.js` + `src/lib/core/{bridge,worker-client}.ts`）✅；直连适配器 `CoreAddonClient`（transport）✅；浏览器端到端（headless Chrome + CDP 四桥直连）✅；浏览器存储 IndexedDB（worker `storage_*` + `CoreStorageClient`）✅；standalone addon 跨源 CORS ✅（SDK `router()`，解挡浏览器直连）；shape-bridge 首件 ✅（`catalog-bridge.ts`）；P4 退役方案 ✅（`docs/plans/web-api-retirement-plan.md`）；addon parity 复核 ✅（live M3U/vod 多源/douban 搜索均已落地，剩外沿功能）；媒体代理切面首件 ✅（`addon-media-proxy.ts` 直连构造器）；媒体代理首条接线 ✅（`USE_ADDON_MEDIA_PROXY` 开关，vod 下载侧）；跨源浏览器端到端 ✅（真实 live addon 四桥直连）；Service Worker ✅（`runtime-caching.js`）；live 页数据源 ✅（`addon-live-client.ts`）。续：live addon 多源 parity → 页面切面（live/点播/豆瓣/账户）+ 退役 `/api/proxy/*` → 退役 TS `/api`。evidence：`2026-08-31-stremio-cutover-phase0.md`、`2026-08-31-stremio-p1c-sync-model.md`、`2026-08-31-stremio-wasm-pipeline.md`；core 仓详见其 `.agnir/evidence/`（`stremio-core-pure-progress.md`、`core-addons-dispatch.md`）。