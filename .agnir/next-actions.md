# CineHarbor Next Actions

0. **提交并推送本次 Agnir 初始化**（`AGNIR.yaml` / `AGENTS.md` / `.agnir/` / README 段），当前均为未提交改动。

1. 六仓尚未 push：README 中的 GitHub 链接在 push 后生效，此前以本地目录为准。
2. 按 `docs/PLAN.md` 推进 P0–P6 分阶段计划。
3. 跟进 `docs/PLAN.md` 开放问题 #1（全仓许可证元数据统一）。
4. 各子仓的后续动作由其各自 Agnir `.agnir/next-actions.md` 维护。
