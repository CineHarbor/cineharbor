# Release 1.0.0 execution checkpoint — 2026-09-19

## Authority

Principal supplied `CineHarbor-Release-Ready-100-Plan.md` and explicitly authorized autonomous implementation, correction of omissions, technical decisions, commits/pushes and CI execution toward release-ready **1.0.0**. Final public release remains excluded by the supplied plan.

## Observed source truth

Facade baseline: `3aca6864dcdf316751bf18080a3eaa3ec65b8cdd` (tree `e08184e96db42b556c03fff2462d655161be4023`). Read `AGENTS.md`, `AGNIR.md`, `AGNIR.yaml`, state, next-actions, decisions, README, PLAN, ADR-0006 and plan index before constructing this checkpoint.

Core baseline: `05989fb6f2fe388d8eaeb4128445ac4c61662f5a`; Actions run `35382333866`, job `105721187947`. Logs show `cargo fmt --all -- --check` failing on both Core and sibling SDK files. Subsequent check/test/clippy steps are skipped, so no compiler/test success is inferred.

Existing documents mix a superseded native-RPC target, newer addon cutovers and historical updater deferral. Current state is reconciled to distinguish the target architecture, reported historical implementation and independently observed acceptance.

## Decision applied

Lock release target to 1.0.0. Retain permitted control/application/release APIs with explicit boundaries rather than requiring mechanically zero API routes. Updater is a release blocker even though it was deferred during Web migration. Move noncore RT/music/follow/advanced-cache expansion to explicit post-1.0 scope without excusing broken visible functionality. Keep release-ready false until evidence exists.

## Changes and limits

Add versioned scope/status and a read-only source-audit workflow; replace stale current state/next actions. Source collection is diagnostic only and is not a release gate. No test, installation, production deployment, signing or public release is claimed by this checkpoint. Existing Agnir identity, lineage, provenance and historical decisions are preserved.

Publication must fast-forward the observed baseline; on conflict, re-resolve and reconcile rather than force overwrite. Destination ref and selected lineage must be freshly verified after publication.
